[build]
builder = "nixpacks"
buildCommand = "cd backend/riggingcheck-api && ./mvnw clean package -DskipTests -q"

[deploy]
startCommand = "java -jar backend/riggingcheck-api/target/riggingcheck-api-0.0.1-SNAPSHOT.jar"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
